import { LoginTemplateComponent } from './loginTemplate.component';

describe('Login.Component', () => {
  it('should create an instance', () => {
    expect(new LoginTemplateComponent).toBeTruthy();
  });
});
